<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>

<BorderPane fx:id="mainRoot"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="org.example.controller.MainController"
            stylesheets="@../css/main.css,@../css/accessibility.css,@../css/app-theme.css">

    <left>

        <VBox prefWidth="244" minWidth="228" maxWidth="258"
              spacing="11"
              styleClass="sidebar">

            <padding>
                <Insets top="22" right="14" bottom="16" left="14"/>
            </padding>

            <HBox spacing="12" alignment="CENTER_LEFT">

                <StackPane styleClass="sidebar-logo-mark">
                    <ImageView fitWidth="38"
                               preserveRatio="true"
                               smooth="true">
                        <image>
                            <Image url="@../images/logoSemNome.png"/>
                        </image>
                    </ImageView>
                </StackPane>

                <VBox spacing="2">

                    <Label text="Cidade em Dia"
                           styleClass="logo"/>

                    <Label text="Gestão de Reclamações"
                           styleClass="logoSubtitle"/>

                </VBox>

            </HBox>

            <Separator styleClass="separator"/>

            <Label text="NAVEGAÇÃO"
                   styleClass="sectionTitle"/>

            <Button text="🏠   Início"
                    accessibleText="Início. Atalho Alt mais 1"
                    fx:id="homeButton"
                    onAction="#goHome"
                    styleClass="menuButton"/>

            <Button text="📝   Nova Reclamação"
                    accessibleText="Nova reclamação. Atalho Alt mais 2"
                    fx:id="newComplaintButton"
                    onAction="#goComplaint"
                    styleClass="menuButton"/>

            <Button text="📋   Reclamações"
                    accessibleText="Lista de reclamações. Atalho Alt mais 3"
                    fx:id="complaintsButton"
                    onAction="#goComplaints"
                    styleClass="menuButton"/>

            <Button text="▥   Análises e IA"
                    accessibleText="Análises e inteligência. Atalho Alt mais 7"
                    fx:id="analyticsButton"
                    onAction="#goAnalytics"
                    styleClass="menuButton"/>

            <Button text="🗺   Mapa"
                    accessibleText="Mapa de reclamações. Atalho Alt mais 4"
                    fx:id="mapButton"
                    onAction="#goMap"
                    styleClass="menuButton"/>

            <Button text="◉   Acompanhamento"
                    accessibleText="Acompanhamento dos problemas. Atalho Alt mais 5"
                    fx:id="trackingButton"
                    onAction="#goTracking"
                    styleClass="menuButton"/>

            <Pane VBox.vgrow="ALWAYS"/>

            <Separator styleClass="separator"/>

            <Button text="⚙   Configurações"
                    accessibleText="Configurações e acessibilidade. Atalho Alt mais 6"
                    fx:id="settingsButton"
                    onAction="#goSettings"
                    styleClass="menuButton"/>

            <Button text="ⓘ   Sobre o sistema"
                    accessibleText="Sobre o sistema. Atalho Alt mais zero"
                    fx:id="aboutButton"
                    onAction="#goAbout"
                    styleClass="menuButton"/>

            <Button text="⇄   Trocar usuário"
                    accessibleText="Trocar usuário. Escolher outro perfil para entrar automaticamente"
                    fx:id="switchUserButton"
                    onAction="#switchUser"
                    styleClass="menuButton"/>

            <VBox spacing="4" styleClass="sidebar-user-card">
                <Label fx:id="loggedUserLabel"
                       text="Usuário"
                       styleClass="footerTitle"/>
                <Label fx:id="loggedUserRoleLabel"
                       text="Perfil"
                       styleClass="footer"/>
            </VBox>

            <VBox spacing="3" styleClass="sidebar-version">

                <Label text="Sistema Cidade em Dia"
                       styleClass="footerTitle"/>

                <Label text="Versão 1.0"
                       styleClass="footer"/>

            </VBox>

        </VBox>

    </left>

    <center>

        <StackPane styleClass="content">
            <StackPane fx:id="contentArea"/>
            <VBox fx:id="toastLayer"
                  spacing="10"
                  mouseTransparent="true"
                  pickOnBounds="false"
                  maxWidth="380"
                  StackPane.alignment="TOP_RIGHT">
                <padding>
                    <Insets top="22" right="22" bottom="22" left="22"/>
                </padding>
            </VBox>
        </StackPane>

    </center>

</BorderPane>
